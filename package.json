{
  "name": "ppt-knowledge-motion-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Upload a PPTX, extract teaching knowledge points, and generate interactive HTML animations.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env --watch server.mjs",
    "start": "node --env-file-if-exists=.env server.mjs",
    "test": "node --test"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2",
    "express": "^5.1.0",
    "jszip": "^3.10.1",
    "katex": "^0.18.7",
    "multer": "^2.0.2",
    "pdfjs-dist": "^6.3.289"
  }
}
