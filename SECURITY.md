# 公开版安全说明

## 已排除

- `.env`、`.data/`、上传文件、生成成果缓存。
- `audit/` 中的截图、真实模型输出、课程和个人试用记录。
- 比赛申报文件、联系方式、私人目录、学校内部地址和登录信息。
- 依赖目录、历史压缩包、个人电脑专用验证脚本。

`.env.example` 只含空密钥及占位服务地址。

## 不要直接暴露完整后端

当前版本是单用户本地应用，没有完整的多用户账户隔离、公开访问鉴权、滥用限流和费用硬上限。模型设置接口属于本机管理功能。

默认仅监听 127.0.0.1。不要仅修改 HOST=0.0.0.0 就向公网开放，也不要将公网请求转发到个人电脑。云端试用前需要登录/访问控制、禁用或保护管理接口、任务数据隔离、上传限制、预算与调用配额、持久化存储、备份与数据删除策略。

密钥在服务器环境或配置文件中保存，并不代表匿名用户无法耗用该密钥的额度。基础设施免费也不代表模型推理免费。

如发现已泄露的真实密钥，应首先在供应商控制台吊销并轮换。删除仓库文件不能清除已经传播的凭据。
